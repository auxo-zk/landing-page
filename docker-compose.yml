version: '3.8'

services:
    frontend-committee:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - '3000:3000'
        environment:
            NODE_ENV: production # Set your desired Node environment